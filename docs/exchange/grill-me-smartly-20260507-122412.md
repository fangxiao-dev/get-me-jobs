# grill-me-smartly review

## Source

- Task: Review the explicit reject preference update implementation plan before implementation.
- Source file/context: `docs/plans/2026-05-07-reject-preference-update.md`
- Created at: `2026-05-07T12:24:12.2146919+02:00`

## Plan Snapshot

The plan adds `npm run preferences:update-rejects -- <date>` for proposal-only reject-rule analysis and `--apply` for confirmed updates to `config/preferences.linkedin.json`. It extracts shared selection helpers, adds pure proposal/apply logic, adds a CLI wrapper, tests, README updates, and `.gitignore` coverage for proposal artifacts.

## Proposed Questions

| ID | Question | Main-session recommended answer |
| --- | --- | --- |
| Q1 | Should apply-mode validation use only `preferences.version`, or should the proposal include and validate a hash/snapshot of `config/preferences.linkedin.json` to prevent applying a proposal after same-version rule edits? | Add a content hash. Treat version as schema unless explicitly changed. |
| Q2 | Should the plan treat `preferences.version` as schema version and leave it unchanged, or increment it on every applied preference content change? | Leave it as schema version for V1 and add `preferencesHash` to proposal inputs. |
| Q3 | Should selected-output regeneration in apply mode force a rewrite even when selected IDs are unchanged, so `data/selected/<date>.json` captures the new exclude match details and updated preferences context? | Yes. Add a `forceWrite` option or equivalent to `selectJobsFile` for apply mode. |
| Q4 | Should proposal mode include `wouldRemoveSelectedJobIds` and accepted/maybe conflict IDs in the proposal summary, not just evidence/warnings counts, so review can verify impact without re-running code? | Yes. Add impact arrays to the proposal artifact. |
| Q5 | Is the candidate extraction strategy too broad because it tokenizes description/title words and may propose generic single-word excludes; should V1 limit generated terms to explicit notes/tags plus structured fields only? | Yes. Remove free title/description token mining from V1. |
| Q6 | Should explicit tags like `too_lab` be normalized into proposal terms at all, or should only tags with a namespace/value format such as `exclude:chemistry synthesis` or `bad_industry:Marketing Services` become rule terms? | Require namespaced tags/notes for explicit term creation. Plain tags can remain evidence labels. |
| Q7 | Should the CLI support multiple dates in V1 (`--since` or repeated dates), or is single-date enough given the current design? | Single-date is enough for V1. |
| Q8 | Should missing `data/selected/<date>.json` be regenerated automatically in proposal mode, or should the command fail and require the user to run selection explicitly to preserve the review state being analyzed? | Fail in proposal mode unless a future explicit `--regenerate-selected` flag is added. |
| Q9 | Should tests assert that default proposal mode never modifies real/tracked preference files by running only in temp directories, or is a runtime `git diff` manual verification enough? | Use temp-dir automated tests; keep manual `git diff` as extra verification. |
| Q10 | Should the implementation plan be amended before execution to fix the test-file issue where `import` statements are appended mid-file in Task 4, which is invalid ESM syntax? | Yes. This is an execution blocker. |

## Subagent Classification

| ID | Classification | Reason |
| --- | --- | --- |
| Q1 | 非常有必要 | Stale-proposal validation affects apply safety and proposal schema. |
| Q2 | 非常有必要 | Defines whether `preferences.version` is schema or content version, which changes validation semantics. |
| Q3 | 非常有必要 | Determines apply-mode artifact behavior and acceptance criteria for regenerated selected output. |
| Q4 | 非常有必要 | Proposal reviewability depends on showing concrete impact IDs before apply. |
| Q5 | 非常有必要 | Candidate extraction breadth directly affects false-positive risk and V1 safety. |
| Q6 | 非常有必要 | Tag normalization defines the rule-generation data contract. |
| Q7 | 需要再讨论 | Multi-date support is scope expansion; single-date V1 is reversible. |
| Q8 | 非常有必要 | Auto-regenerating missing selected data may analyze a different review state. |
| Q9 | 非常有必要 | Test isolation is required to prove proposal mode cannot mutate tracked preferences. |
| Q10 | 非常有必要 | Invalid mid-file ESM imports are an execution blocker in the plan. |

## Decision Packet

- 非常有必要: Q1, Q2, Q3, Q4, Q5, Q6, Q8, Q9, Q10.
- 需要再讨论: Q7.
- Recommended decision: amend the plan before implementation. Keep V1 single-date, add preference content-hash validation, force selected-output rewrite on apply, include impact IDs in proposal artifacts, restrict V1 extraction to structured fields and explicit namespaced terms, fail on missing selected output in proposal mode, and fix the ESM test import ordering.

